/**
 * File: charts.js
 * Purpose: ECharts base options (we'll flesh out later)
 */
export const BASE_ECHARTS = {
    backgroundColor: 'transparent',
    textStyle: { fontFamily: getComputedStyle(document.documentElement).getPropertyValue('--ASFontPrimary') }
  };
  